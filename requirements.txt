# Deployed serverless backend (lightweight — no GPU deps)
fastapi
numpy
pillow

# --- Reference only (not deployed) ---
# The following are needed when real ML models are plugged in:
# torch
# open_clip_torch
# transformers
# rasterio
# uvicorn
